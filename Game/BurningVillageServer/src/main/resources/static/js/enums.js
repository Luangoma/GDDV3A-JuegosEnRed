const MULTIPLAYER_TYPE = Object.freeze({
	LOCAL: 0,
	ONLINE: 1
});

const CAMERA_SPLIT_TYPE = Object.freeze({
	VERTICAL: 0,
	HORIZONTAL: 1
});

const FORM_TYPE = Object.freeze({
	DEFAULT: 0,
	PASSWORD_ONLY: 1,
	PASSWORD_DOUBLE: 2,
	FULL: 3
});
